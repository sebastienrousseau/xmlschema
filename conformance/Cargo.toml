[package]
name = "xmlschema-conformance"
version = "0.0.8"
edition = "2024"
rust-version = "1.86.0"
publish = false
description = "The W3C XML Schema Test Suite, run against xmlschema."
license = "MIT OR Apache-2.0"

[dependencies]
oxml = "0.0.8"
xmlschema = { path = ".." }

[lints.clippy]
pedantic = "warn"
